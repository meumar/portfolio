/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost:3000', 'meumar.btb.ind.in', 'raw.githubusercontent.com'],
    },
}

module.exports = nextConfig
